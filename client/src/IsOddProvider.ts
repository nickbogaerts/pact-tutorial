/**
 * Provider for the isOdd SaaS solution
 *
 * You will need to implement this once you have your pact written.
 * You should be able to connect it to pact's mock server to try it out too!
 */

const isOddProvider =
  (basePAth: string) =>
  async (input: string): Promise<boolean> => {
    throw new Error(`This functionality has not been implemented yet`);
  };

export default isOddProvider;
